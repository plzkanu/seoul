# 개발 환경 (.env 로 복사하여 사용)
NODE_ENV=development
PORT=3000

# JWT 서명용 비밀 키 (배포 시 반드시 설정, 강한 랜덤 문자열 사용)
# JWT_SECRET=your-secret-key-here

# DB 파일 경로 (기본: server/approval.db)
# DATABASE_PATH=./server/approval.db
