# 배포 환경 (Replit Secrets에 설정)
NODE_ENV=production
PORT=3000

# 필수: JWT 서명용 비밀 키 (강한 랜덤 문자열 사용)
JWT_SECRET=your-production-secret-key

# 선택: DB 파일 경로 (Replit 등에서 영구 저장 경로 지정 시)
# DATABASE_PATH=/home/runner/approval.db
